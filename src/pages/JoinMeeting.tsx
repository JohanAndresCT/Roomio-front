const JoinMeeting: React.FC = () => {
  return (
    <section aria-labelledby="join-heading">
      <h1 id="join-heading">Join Meeting</h1>
      <p>Enter meeting ID or link to join. Layout-only for Sprint 1.</p>
    </section>
  )
}

export default JoinMeeting
